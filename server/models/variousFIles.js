import mongoose from "mongoose";

const variousSchema = mongoose.Schema({
  // pliki pdf/doc/archiwum...
  // tu pomyslec co i jak
});

const VariousSchema = mongoose.model("VariousSchema", variousSchema);

export default VariousSchema;
